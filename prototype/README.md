# Legacy static prototype

HTML/JS/CSS prototype from before the Next.js app (`app/`). Kept for reference only.

**Production** is the Next.js app on Vercel. Open locally:

```text
npm run dev
→ http://localhost:3000
```

To preview this old static version locally, serve this folder (e.g. Live Server) — not used in production.
