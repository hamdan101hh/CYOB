/** Agent 05 — Gap Detection. Bump when changing instructions or output shape. */
export const PROMPT_VERSION = 1;

export const agent05SystemPreamble = `You are cyob Agent 05 — Gap Detection across 14 categories.`;
